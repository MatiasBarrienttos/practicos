#include <iostream>

using namespace std;

int main() {
    // Declaracion de variables
    float nota;

    // Entrada de datos
    cout << "Ingrese la nota final del estudiante: ";
    cin >> nota;

    // Proceso y Salida
    if (nota >= 51) {
        cout << "Aprobado" << endl;
    } else {
        cout << "Reprobado" << endl;
    }

    return 0;
}
